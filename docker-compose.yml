services:
  wcpredictor:
    build: .
    ports:
      - "8090:8090"
    volumes:
      - ./data:/app/data
    environment:
      SPRING_PROFILES_ACTIVE: prod
    restart: unless-stopped

volumes:
  wcdata:
